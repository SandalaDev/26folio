---
id: "DEP-20260729-205420-add"
mode: "add"
status: "installed"
human_approval: "approved"
purpose: "Align Agent OS YAML runtime without changing the portfolio stack"
opensrc_cli: "0.7.2"
created_at: "2026-07-29T20:54:20Z"
packages:
  - "yaml@2.9.0"
installed_at: "2026-07-29T21:07:29.784Z"
---

# Dependency evidence: Align Agent OS YAML runtime without changing the portfolio stack

OpenSrc fetched the exact candidate sources before project packages were
installed. OpenSrc is evidence access, not proof of compatibility.

## Candidate evidence

### yaml@2.9.0

- OpenSrc command: `opensrc path yaml@2.9.0 --cwd .`
- Evidence cache used for this review: `C:/Users/abesa/.opensrc/repos/github.com/eemeli/yaml/2.9.0`
- Package manifest: `package.json`
- Engines: `{"node":">= 14.6"}`
- Peer dependencies: `{}`
- Peer dependency metadata: `{}`
- Exports/runtime entry points: `{".":{"types":"./dist/index.d.ts","node":"./dist/index.js","default":"./browser/index.js"},"./package.json":"./package.json","./util":{"types":"./dist/util.d.ts","node":"./dist/util.js","default":"./browser/dist/util.js"}}`
- Documentation inventory: `docs/01_intro.md`, `docs/02_parse_stringify.md`, `docs/03_options.md`, `docs/04_documents.md`, `docs/05_content_nodes.md`, `docs/06_custom_tags.md`, `docs/07_parsing_yaml.md`, `docs/08_errors.md`, `docs/09_cli.md`, `docs/10_yaml_syntax.md`, `docs/CONTRIBUTING.md`, `docs/index.html.md`, `docs/README.md`, `docs/SECURITY.md`, `README.md`

#### Docs and source findings

Reviewed `package.json`, `README.md`, `docs/01_intro.md`,
`docs/02_parse_stringify.md`, `docs/03_options.md`, `docs/08_errors.md`, and
`docs/SECURITY.md` from the OpenSrc-fetched `yaml@2.9.0` source. The package
requires Node.js 14.6 or newer, has no runtime or peer dependencies, and exports
both Node and browser entry points. The Agent OS imports the Node entry from
JavaScript to parse and render repository frontmatter. No migration is required:
the portfolio lockfile already resolves this exact release.

#### Project fit and risks

26folio runs Node.js 22.19.0, so the package engine is satisfied. YAML does not
overlap with the Next.js, React, animation, styling, or deployment runtime; it
is used only by repository tooling. The change replaces the manifest range
`^2.9.0` with the exact version already present in `package-lock.json`, avoiding
application-code and lockfile-version changes. The remaining risk is an Agent
OS regression in frontmatter parsing, covered by OS render and state tests.


## Cross-package compatibility matrix

| Candidate A | Candidate B | Compatibility evidence and constraints |
|---|---|---|
| yaml@2.9.0 | Node 22.19.0, Next 15.5.19, React 19.1.0, existing lockfile | Node engine satisfied; no runtime or peer dependencies; existing lock already resolves 2.9.0; npm dry-run accepted the graph without warnings |

## Combined architecture and best-practice conclusion

Keep YAML isolated to Agent OS scripts and repository frontmatter. Application
configuration remains owned by Next.js and TypeScript; content and UI data
remain ordinary TypeScript or Markdown. No package has overlapping ownership,
and no existing project convention changes.

## Package-manager compatibility result

The pre-install resolver ran while this plan was created. It installed no package
payloads.

- Result: **accepted**
- Exit status: `0`
- Standard output: `up to date in 3s 143 packages are looking for funding run 'npm fund' for details`
- Warnings/errors: none

The resolver accepted the exact candidate against the complete existing npm
graph and emitted no warnings. Combined with the already-matching lockfile,
independent engine range, and absence of peers, this is sufficient pre-install
graph evidence. OS rendering and the portfolio build remain the runtime proof.

## Verification after installation

Run `os check`, `os doctor`, the derived-state and dependency tests, then run
26folio's `lint`, `typecheck`, and production `build`. Render the dashboard and
guide and confirm the migrated legacy task inventory is visible.

## Human decision

AGENT: after completing every required field, change `status` to
`review-ready`.

HUMAN: review the evidence and exact versions. If approved, change `status` to
`approved` and `human_approval` to `approved`. The OS install command will
not run this plan until then. Direct package-manager commands remain available
because the OS does not gate pushes.
